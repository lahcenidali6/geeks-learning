
[1, 2, 3].map(num => {
  if (typeof num === 'number') return num * 2; //check type of variable if its number and then return num*2 
  return ; //if the variable not number return nothing
})
//the output will be : [2,4,6]